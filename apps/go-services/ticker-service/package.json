{
  "name": "ticker-service",
  "scripts": {
    "dev": "go build -o ../bin/ticker-service main.go && go run main.go",
    "build": "go build -o ../bin/ticker-service main.go"
  },
  "dependencies": {
    "@repo/proto": "workspace:*"
  }
}
